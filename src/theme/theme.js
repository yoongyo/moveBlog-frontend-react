import { COLORS } from "../colors/color"

export const dark = {
    colors:{
        bgColor: COLORS.darkBackground,
        text: 'white'
    }
}

export const light = {
    colors:{
        bgColor: COLORS.lightBackground,
        text: 'black'
    }
}